---
const { as = 'div', size = 'default', class: className = '' } = Astro.props;
const Tag = as;
---

<Tag class={`container ${size !== 'default' ? `container--${size}` : ''} ${className}`.trim()}>
  <slot />
</Tag>
